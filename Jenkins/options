pipeline {
agent { label 'slave1' }

options {
timeout(time: 1, unit: 'SECONDS')
disableConcurrentBuilds()
timestamps()
}

stages {

stage ('TEST') {
steps {
sh """
pwd
ls -lrt
"""
}
}

stage ('DEPLOY') {
steps {
echo "Test message"
}
}
}
}
